{
  "name": "typescript-problems-practice",
  "version": "1.0.0",
  "description": "1.  Convert the following JavaScript array into a TypeScript tuple\r ```\r     const userInfo = [101, \"Ema\", \"John\", true,  , \"2023\"];\r ```\r 2.  Write a TypeScript function that takes in two arrays of numbers as parameters. The function should compare the elements in both arrays and return a new array that contains only the elements that are present in both arrays.",
  "main": "index.js",
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
